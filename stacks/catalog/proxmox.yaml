components:
  terraform:
    proxmox/defaults:
      metadata:
        component: "proxmox"
        type: "abstract"
      vars:
        proxmox_api_token_secret: !env PROXMOX_API_TOKEN_SECRET
        container_password: !env CONTAINER_PASSWORD
