components:
  packer:
    packer/proxmox/debian-11:
      metadata:
        component: "proxmox/debian-11"
      vars:
        proxmox_api_user: !env PROXMOX_USERNAME
        proxmox_api_password: !env PROXMOX_PASSWORD

