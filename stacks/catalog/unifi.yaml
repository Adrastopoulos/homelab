components:
  terraform:
    unifi/defaults:
      metadata:
        component: "unifi"
        type: "abstract"
      vars:
        unifi_password: !env UNIFI_PASSWORD

        wifi_password: !env WIFI_PASSWORD
        guest_wifi_password: !env GUEST_WIFI_PASSWORD
        iot_wifi_password: !env IOT_WIFI_PASSWORD
